{
  "name": "rangle-starter",
  "version": "1.4.1",
  "description": "A command-line utility to create a repo based on one of Rangle.io's standard tech stacks.",
  "repository": "https://github.com/rangle/rangle-starter",
  "preferGlobal": true,
  "bin": {
    "rangle-starter": "./index.js"
  },
  "engines": {
    "node": ">=4"
  },
  "author": "rangle.io",
  "license": "MIT",
  "scripts": {
    "test": "npm run lint",
    "lint": "eslint .",
    "release": "git commit -am $npm_package_version && git tag $npm_package_version && git push upstream && npm publish && git push upstream --tags"
  },
  "dependencies": {
    "inquirer": "^1.0.0",
    "inquirer-promise": "0.0.3",
    "rimraf": "^2.5.2"
  },
  "devDependencies": {
    "babel-eslint": "^6.0.4",
    "eslint": "^3.0.1",
    "eslint-plugin-react": "^6.0.0"
  }
}
