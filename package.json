{
  "name": "rangle-starter",
  "version": "1.0.1",
  "description": "A command-line utility to create a repo based on one of Rangle.io's standard tech stacks.",
  "repository": "https://github.com/rangle/rangle-starter",
  "preferGlobal": true,
  "bin": {
    "rangle-starter": "./index.js"
  },
  "engines": {
    "node": "4.x"
  },
  "author": "rangle.io",
  "license": "MIT",
  "scripts": {
    "test": "npm run lint",
    "lint": "eslint .",
    "release": "git commit -am $npm_package_version && git tag $npm_package_version && git push && npm publish && git push --tags"
  },
  "dependencies": {
    "inquirer": "^0.12.0",
    "inquirer-promise": "0.0.3",
    "rimraf": "^2.5.2"
  },
  "devDependencies": {
    "eslint": "^2.2.0"
  }
}
